# O servidor é acessível somente neste computador.
PORT=3001
# DATABASE_PATH=data/reqaudit.sqlite
# ATTACHMENTS_PATH=data/attachments
# SESSION_SECRET=gere-uma-chave-longa-e-aleatoria

# Também pode ser configurado por um administrador na interface.
# Mantenha false até cadastrar endereços reais para os responsáveis.
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=ReqAudit <reqaudit@example.test>
